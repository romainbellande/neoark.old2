import React from 'react';

import ResourceBar from './ResourceBarContainer';

export default { title: 'ResourceBar|ResourceBar', component: ResourceBar };

export const Default = () => <ResourceBar />;
